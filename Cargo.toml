[package]
name = "busd"
version = "0.5.0"
authors = ["Zeeshan Ali Khan <zeeshanak@gnome.org>"]
description = "A D-Bus bus (broker) implementation"
edition = "2021"
license = "MIT"
readme = "README.md"
rust-version = "1.74"
repository = "https://github.com/z-galaxy/busd"
keywords = ["D-Bus", "DBus", "IPC"]
categories = ["network-programming"]
exclude = ["LICENSE"]

[lib]
name = "busd"
path = "src/lib.rs"

[[bin]]
name = "busd"
path = "src/bin/busd.rs"

[dependencies]
# TODO: Switch back to a crates-io version once zbus releases a version with
# `Builder::build_message_stream`. See https://github.com/z-galaxy/zbus/pull/1760.
zbus = { git = "https://github.com/z-galaxy/zbus", rev = "72c72193355dfed210f16990bd495b1b671a1968", features = [
    "tokio",
    "bus-impl",
    "service",
], default-features = false }
tokio = { version = "1.37.0", features = [
    "macros",
    "rt-multi-thread",
    "signal",
] }
clap = { version = "4.5.4", features = [
    "derive",
    "std",
    "help",
], default-features = false }
tracing = { version = "0.1.40", optional = true }
tracing-subscriber = { version = "0.3.18", features = [
    "fmt",
    "ansi",
], default-features = false, optional = true }
anyhow = "1.0.82"
# Explicitly depend on serde to enable `rc` feature.
serde = { version = "1.0.200", features = ["rc"] }
futures-util = { version = "0.3.30", default-features = false }
enumflags2 = "0.7.9"
console-subscriber = { version = "0.5.0", optional = true }
xdg-home = "1.1.0"
event-listener = "5.3.0"
fastrand = "2.2.0"
quick-xml = { version = "0.42.0", features = ["serialize"] }

rustix = { version = "1.1.2", default-features = false, features = ["process"] }

[features]
default = ["tracing-subscriber"]
tracing = ["dep:tracing", "zbus/tracing", "tokio/tracing"]
tracing-subscriber = ["tracing", "dep:tracing-subscriber"]
console-subscriber = ["tracing", "dep:console-subscriber"]

[dev-dependencies]
# The tests drive the bus through proxies, which the binary itself does not need.
zbus = { git = "https://github.com/z-galaxy/zbus", rev = "72c72193355dfed210f16990bd495b1b671a1968", features = [
    "proxy",
], default-features = false }
ntest = "0.9.2"
rand = "0.10.0"
futures-util = { version = "0.3.30", default-features = true }
tracing = "0.1.40"

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = "s"
panic = "abort"
# generates a separate *.dwp/*.dSYM so the binary can get stripped
split-debuginfo = "packed"
strip = "symbols"
# No one needs an undebuggable release binary
debug = "full"
